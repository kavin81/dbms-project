version: "3.8"

services:
  mysql:
    image: mysql:8.3
    container_name: mysql
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
    networks:
      - internal

networks:
  internal:
    driver: bridge
